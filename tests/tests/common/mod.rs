// common/mod.rs is an alternative naming convention that
// Rust also understands. Naming the file this way tells Rust
// NOT to treat the common module as an integration test file.

pub fn setup() {
    // setup code specific to your library's tests would go here.
}
